Feature:NewGetIdClick -->Logout and Close the browser
  Here we want to logout and close the browser using the automation.

Scenario:Logout and close the browser.
  Then click on the "Logout" button.
  Then close the browser.